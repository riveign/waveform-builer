/** Shared icon size scale for inline-SVG icon primitives. Maps to the
 *  Tier-3 --icon-size-* tokens (tokens.semantic.css, content-conventions §6:
 *  one size scale, one stroke weight). */
export type IconSize = 'sm' | 'md' | 'lg';
