# Commits that changed formatting only. `git blame` skips these so it keeps
# pointing at whoever last changed the *meaning* of a line.
#
# Enable locally (once):
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub honours this file automatically.

e5199164dceac08d4522174d9cb8aca3996593ca  # style: ruff format the codebase
